FROM golang:1.25-alpine AS builder
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN go build -o /event-adapter ./cmd/event-adapter

FROM alpine:3.20
COPY --from=builder /event-adapter /usr/local/bin/event-adapter
ENTRYPOINT ["event-adapter"]
