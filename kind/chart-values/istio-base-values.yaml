# istio-base chart — Istio core CRDs and cluster-wide RBAC
# Vendored at: kind/charts/base-1.24.2.tgz
# Chart name:    base
# Chart version: 1.24.2
#
# Container images deployed by this chart: NONE.
# This chart installs Istio CRDs (Gateway, VirtualService, EnvoyFilter, etc.)
# and cluster-wide ServiceAccounts/ClusterRoles only — no pods or images.
#
# No image registry/tag to declare; left here for parity with the other
# chart-values files and as a place to drop CRD-related overrides if needed.

defaultRevision: default
